/** Прибавка к радиусу обзора наземного юнита или здания на холме. */
export const HILL_SIGHT_BONUS = 1;

/**
 * Память о вражеских юнитах вне обзора, в полных кругах ходов.
 * Старт; подбирается в S21. Память здания живёт до проверки места.
 */
export const CONTACT_MEMORY = {
  /** После стольких кругов контакт считается устаревшим. */
  staleAfter: 1,
  /** После стольких кругов контакт с юнитом забывается. */
  forgetAfter: 3,
};

/**
 * Предполагаемая цена входа в неразведанную клетку при планировании
 * маршрута ИИ на несколько ходов: выше цены поля, чтобы маршрут предпочитал
 * известную местность. Приказ на текущий ход оценивает её наименьшей ценой.
 */
export const UNKNOWN_MOVE_COST = 2;
