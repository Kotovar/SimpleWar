# Карта, ход и экономика

## Начало партии

В настройках независимо выбираются размер Canvas (600, 720, 840 или 1200 пикселей
по каждой стороне) и размер сетки (15×15, 24×24, 30×30 или 40×40).
По умолчанию — 840×840 пикселей, 30×30 клеток, случайная генерация.
Для фиксированного режима начальный сид — `0.15`.

Каждая сторона получает ратушу, рабочего, **200 золота и 120 дерева**.
Ратуши стоят в `(1, 1)` и `(cols − 2, rows − 2)` при отсчёте координат с нуля.
Вокруг них расчищается участок 3×3. Начальная вместимость — 10,
занято стартовым рабочим — 1.

## Местность

| Тип               | Проходимость       | Назначение                            |
| ----------------- | ------------------ | ------------------------------------- |
| Поле (`grass`)    | Да, если не занято | Движение, найм, фермы, казармы, башни |
| Лес (`forest`)    | Нет                | Место для лесопилки                   |
| Золото (`gold`)   | Нет                | Место для золотого рудника            |
| Гора (`mountain`) | Нет                | Препятствие                           |
| Вода (`water`)    | Нет                | Препятствие                           |

Рельеф создаётся через simplex-noise, золото размещается отдельными кластерами.
В случайном режиме генератор до 10 раз пытается найти карту с путём между
стартовыми точками; если не удалось, оставляет последнюю. Подготовка стартовых
участков выполняется после этой проверки. Гарантии проходимости нет.

## Ходы и победа

1. После настройки начинается ход игрока № 1 (`inProgress`).
2. При завершении хода владельцу начисляется доход его зданий.
3. Восстанавливаются очки перемещения, атаки, строительства и найма
   **обеих сторон**, затем меняется активная сторона.
4. ИИ сразу завершает свой ход. После него увеличивается номер хода.
5. Уничтожение ратуши переводит игру в `gameOver` и назначает победителя.

Управление — выбор объектов и подсвеченных клеток мышью, кнопки строительства,
найма, завершения хода и сброса. Состояние партии хранится в памяти приложения.

## Ресурсы и население

- Ресурсы: золото и дерево. Рабочие не добывают их вручную; доход дают здания.
- Ратуша приносит 3 золота, рудник — 15 золота, лесопилка — 15 дерева за свой ход.
- Вместимость: сумма снабжения ратуш и ферм, но не более **30**.
- Рабочий занимает 1 слот, каждый военный — 2. При найме проверяется новый итог.
- Разрушение здания снабжения уменьшает вместимость, но существующие юниты
  не удаляются; новый найм блокируется, если для него не хватает мест.
- Содержание армии, еда как ресурс и истощение месторождений не реализованы.

Источники: [настройки карты](../src/shared/config/map.ts),
[старт](../src/widgets/start-game/useStartGame.tsx),
[смена хода](../src/features/game-loop/lib/gameLoopActions.ts),
[экономика](../src/entities/economies/model/economyStore.ts),
[события населения](../src/app/system/population.ts).
